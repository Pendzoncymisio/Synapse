# Synapse Protocol Requirements
# Python dependencies for the OpenClaw HiveBrain torrentlike skill

# Core requirements (these would be installed in production)
# For now, keeping it minimal with standard library focus

# Embeddings - Local computation with Nomic Embed
sentence-transformers>=2.3.0
optimum[onnxruntime]>=1.16.0
onnxruntime>=1.16.0

# Post-Quantum Cryptography (ML-DSA-87 via OpenSSL, fallback to Ed25519)
cryptography>=41.0.0  # For Ed25519 fallback

# Data validation
dataclasses-json>=0.6.0

# Networking (for real P2P implementation)
# libtorrent>=2.0.0  # Uncomment when ready for production P2P
# aiohttp>=3.9.0     # For async HTTP tracker communication
# bencodepy>=0.9.5   # For BitTorrent bencode format

# Vector database support (optional, depending on use case)
# faiss-cpu>=1.7.4   # For FAISS vector stores
# lancedb>=0.3.0     # For LanceDB vector stores
# chromadb>=0.4.0    # For ChromaDB vector stores

# Safety and security
# regex>=2023.0.0    # Enhanced regex for pattern matching
# cryptography>=41.0.0  # For encryption and signature verification

# Utilities
# python-magic>=0.4.27  # File type detection
# tqdm>=4.66.0          # Progress bars for downloads

# Development dependencies (not needed in production)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# black>=23.0.0
# mypy>=1.5.0
